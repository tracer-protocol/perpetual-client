import React from 'react';
import NavBar from '@components/Nav/NavBar';

const Home: React.FC = () => {
    return (
        <React.Fragment>
            <NavBar />
            <div>Home</div>
        </React.Fragment>
    );
};

export default Home;
